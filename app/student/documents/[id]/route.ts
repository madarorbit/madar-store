import{NextResponse}from'next/server';import{requireUser}from'@/src/lib/auth';import{supabaseFetch,serverToken}from'@/src/lib/supabase/server';import{supabaseConfig}from'@/src/lib/env';
export const dynamic='force-dynamic';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){await requireUser();const{id}=await params,rows=await supabaseFetch(`/rest/v1/student_documents?id=eq.${encodeURIComponent(id)}&select=storage_path,original_filename`),document=rows?.[0];if(!document)return new NextResponse(null,{status:404});const{url,key}=supabaseConfig(),token=await serverToken(),response=await fetch(`${url}/storage/v1/object/authenticated/student-library/${document.storage_path}`,{headers:{apikey:key,Authorization:`Bearer ${token}`},cache:'no-store'});if(!response.ok)return new NextResponse(null,{status:404});const safe=String(document.original_filename).replace(/["\r\n]/g,'');return new NextResponse(response.body,{headers:{'Content-Type':'application/pdf','Content-Disposition':`inline; filename="${safe}"`,'Cache-Control':'private, no-store'}})}
