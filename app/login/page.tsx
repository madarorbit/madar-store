import PageShell from '@/components/ui/PageShell';import { PageHero,Section } from '@/components/ui/Section';import { AuthForm } from '@/components/auth/AuthForm';export default async function Page({searchParams}:{searchParams:Promise<{next?:string}>}){const q=await searchParams;return <PageShell><PageHero eyebrow="حساب مَدار" title="تسجيل الدخول" description="ادخل إلى حسابك بأمان."/><Section><AuthForm kind="login" next={q.next}/></Section></PageShell>}
