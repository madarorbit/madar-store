import type { MetadataRoute } from 'next';import { siteConfig } from '@/src/config/site';import { products } from '@/src/data/products';const routes=['','products','services','about','contact','faq','privacy','terms','checkout'];export default function sitemap():MetadataRoute.Sitemap{return[...routes.map(r=>({url:`${siteConfig.baseUrl}/${r}`.replace(/\/$/,'/'),lastModified:new Date('2026-07-19'),changeFrequency:'monthly' as const,priority:r?0.7:1})),...products.map(p=>({url:`${siteConfig.baseUrl}/products/${p.slug}`,lastModified:new Date('2026-07-19'),changeFrequency:'monthly' as const,priority:.8}))]}
