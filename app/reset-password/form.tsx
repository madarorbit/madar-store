'use client';import{useActionState}from'react';import{resetPassword}from'@/app/actions/auth';
export default function ResetForm(){const[state,action,pending]=useActionState<{success?:string;error?:string},FormData>(resetPassword,{});return <form action={action} className="mx-auto max-w-md space-y-4 rounded-3xl border border-white/10 p-6"><label className="block">كلمة المرور الجديدة<input required minLength={8} name="password" type="password" className="field mt-1 w-full rounded-xl p-3"/></label><label className="block">تأكيد كلمة المرور<input required minLength={8} name="confirm" type="password" className="field mt-1 w-full rounded-xl p-3"/></label>{state.error&&<p role="alert" className="text-red-300">{state.error}</p>}{state.success&&<p className="text-emerald-300">{state.success}</p>}<button disabled={pending} className="w-full rounded-xl bg-white p-3 text-black">{pending?'جارٍ الحفظ…':'تغيير كلمة المرور'}</button></form>}
