import Link from'next/link';import{redirect}from'next/navigation';import{requireUser}from'@/src/lib/auth';import{currentProfile,supabaseFetch}from'@/src/lib/supabase/server';import{organizationTypeLabels,type OrganizationType}from'@/src/lib/workspace';
export const dynamic='force-dynamic';
export default async function Page(){const user=await requireUser(),profile=await currentProfile();const rows=await supabaseFetch(`/rest/v1/organization_members?user_id=eq.${encodeURIComponent(user.id)}&select=role,organizations(id,name,slug,type,status)`),membership=rows?.[0],workspace=membership?.organizations;if(!workspace){const requests=await supabaseFetch(`/rest/v1/workspace_requests?user_id=eq.${user.id}&select=id,name,status,rejection_reason&order=created_at.desc&limit=1`),request=requests?.[0];if(!request)redirect('/onboarding');if(request.status==='pending_payment')redirect(`/workspace-payment/${request.id}`);return <main className="mx-auto max-w-3xl p-6 py-12"><h1 className="text-3xl font-black">حالة طلب مساحة العمل</h1><section className="mt-6 rounded-3xl border border-white/10 bg-white/[.04] p-6"><h2 className="text-xl font-bold">{request.name}</h2>{request.status==='pending_review'?<p className="mt-4 leading-8 text-amber-100">الدفع قيد المراجعة. ستفتح مساحتك تلقائيًا بمجرد موافقة إدارة مَدار.</p>:<><p className="mt-4 text-red-200">تم رفض الطلب: {request.rejection_reason||'يرجى التواصل مع الدعم.'}</p><Link href="/contact" className="mt-4 inline-block font-bold text-[#70E4D4]">تواصل مع الدعم</Link></>}</section></main>}if(workspace.type==='STUDENT')redirect('/student');const complete=Boolean(profile?.full_name&&profile?.phone);return <main className="mx-auto max-w-5xl p-6"><p className="text-[#00C292]">مساحة العميل</p><h1 className="mt-2 text-4xl font-bold">مرحباً {profile?.full_name||'بك'}،</h1><section className="mt-8 rounded-3xl border border-white/10 bg-white/[.04] p-6"><h2 className="text-2xl font-bold">{workspace.name}</h2><p className="mt-2 text-slate-300">{organizationTypeLabels[workspace.type as OrganizationType]} · دورك: {membership.role}</p><p className="mt-2 text-slate-400">حالة المساحة: {workspace.status} · البريد: {user.email_confirmed_at?'مؤكد':'بانتظار التأكيد'}</p></section><section className="mt-6 grid gap-4 md:grid-cols-2"><article className="rounded-2xl border border-white/10 p-5"><h2 className="font-bold">خطوات البداية</h2><ul className="mt-3 space-y-2 text-slate-300"><li>{complete?'✓':'○'} استكمال الملف الشخصي</li><li>✓ إنشاء مساحة عمل</li><li>○ إضافة أعضاء عند الحاجة</li></ul></article><article className="rounded-2xl border border-white/10 p-5"><h2 className="font-bold">روابط سريعة</h2><div className="mt-3 flex flex-wrap gap-3"><Link className="rounded-xl bg-white px-4 py-2 text-black" href="/account/profile">الملف الشخصي</Link><Link className="rounded-xl border border-white/20 px-4 py-2" href="/account/business">إعدادات التجارة</Link></div></article></section></main>}
