import{notFound}from'next/navigation';import Link from'next/link';import PageShell from'@/components/ui/PageShell';import{Section}from'@/components/ui/Section';import{Breadcrumbs}from'@/components/ui/Breadcrumbs';import{supabaseFetch}from'@/src/lib/supabase/server';import{catalogImageUrl}from'@/src/lib/catalog-media';import{Icon}from'@/components/ui/Icons';import AddToCart from'@/components/cart/AddToCart';export const dynamic='force-dynamic';
export default async function Page({params}:{params:Promise<{slug:string}>}){let service;try{service=(await supabaseFetch(`/rest/v1/services?slug=eq.${encodeURIComponent((await params).slug)}&status=eq.published&select=*`))?.[0]}catch{throw new Error('تعذر تحميل الخدمة.')}if(!service)notFound();const image=catalogImageUrl(service.thumbnail_url);return <PageShell><Section><Breadcrumbs items={[{label:'المتجر',href:'/store'},{label:'الخدمات',href:'/services'},{label:service.name}]}/><div className="grid items-start gap-10 lg:grid-cols-[1fr_390px]"><article><div className="relative mb-8 aspect-[16/8] overflow-hidden rounded-[2rem] border border-white/10 bg-[#11182B]">{image?<div className="absolute inset-0 bg-cover bg-center" style={{backgroundImage:`url("${image.replace(/["\\]/g,'')}")`}} role="img" aria-label={`صورة خدمة ${service.name}`}/>:<div className="absolute inset-0 grid place-items-center bg-[radial-gradient(circle_at_30%_20%,rgba(108,59,255,.6),transparent_35%),radial-gradient(circle_at_70%_75%,rgba(0,194,168,.45),transparent_38%)]"><Icon name="automation" className="h-24 w-24"/></div>}</div><p className="font-bold text-[#70E4D4]">خدمة تنفيذ من مَدار</p><h1 className="mt-3 text-4xl font-black leading-tight sm:text-6xl">{service.name}</h1><p className="mt-6 max-w-3xl text-lg leading-9 text-slate-300">{service.long_description||service.short_description}</p><h2 className="mt-10 text-2xl font-black">مخرجات الخدمة</h2><ul className="mt-5 grid gap-3 sm:grid-cols-2">{(service.features||[]).map((f:string)=><li key={f} className="flex gap-3 rounded-2xl border border-white/10 p-4 text-slate-300"><Icon name="check" className="h-5 w-5 shrink-0 text-[#70E4D4]"/>{f}</li>)}</ul></article><aside className="rounded-3xl border border-white/10 bg-white/[.05] p-6 lg:sticky lg:top-24"><p className="text-sm text-slate-400">السعر التقديري</p><p className="mt-2 text-3xl font-black text-[#70E4D4]">يبدأ من {Number(service.price_from).toLocaleString('ar-SA')} {service.currency}</p><p className="mt-5 leading-7 text-slate-300">تُعتمد التفاصيل النهائية بعد مراجعة احتياجك.</p><div className="mt-6"><AddToCart item={{id:service.id,type:'service',title:service.name,price:Number(service.price_from),currency:service.currency,slug:service.slug}} label="أضف الخدمة إلى السلة"/></div><Link href="/cart" className="mt-3 block text-center text-sm font-bold text-[#70E4D4]">عرض السلة</Link><Link href="/service-agreement" className="mt-4 block text-center text-sm text-slate-400 hover:text-white">اتفاقية تقديم الخدمة</Link></aside></div></Section></PageShell>}
