import PageShell from'@/components/ui/PageShell';import{PageHero,Section}from'@/components/ui/Section';import{requireUser}from'@/src/lib/auth';import{supabaseFetch}from'@/src/lib/supabase/server';export const dynamic='force-dynamic';export const metadata={title:'مكتبتي الرقمية'};
export default async function Page(){await requireUser();const items=await supabaseFetch('/rest/v1/order_items?item_type=eq.product&select=id,title,product_id,orders!inner(payment_status)&orders.payment_status=eq.approved&order=created_at.desc');return <PageShell><PageHero eyebrow="مشترياتي" title="المكتبة الرقمية" description="تنزيل آمن ومؤقت للمنتجات التي تم اعتماد دفعها."/><Section>{!items?.length?<div className="rounded-3xl border border-white/10 bg-white/[.04] p-10 text-center">لا توجد منتجات رقمية مدفوعة بعد.</div>:<div className="grid gap-5 md:grid-cols-2">{items.map((x:any)=><article key={x.id} className="rounded-3xl border border-white/10 bg-white/[.04] p-6"><p className="text-sm font-bold text-[#70E4D4]">منتج رقمي</p><h2 className="mt-2 text-xl font-black">{x.title}</h2><a href={`/account/purchases/${x.id}/download`} className="mt-5 inline-flex rounded-xl bg-white px-5 py-3 font-bold text-slate-950">تنزيل الملف</a></article>)}</div>}</Section></PageShell>}
