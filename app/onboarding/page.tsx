import{requireUser}from'@/src/lib/auth';import{supabaseFetch}from'@/src/lib/supabase/server';import{redirect}from'next/navigation';import OnboardingForm from'./form';
export const dynamic='force-dynamic';export default async function Page(){const user=await requireUser();const memberships=await supabaseFetch(`/rest/v1/organization_members?user_id=eq.${user.id}&select=organization_id&limit=1`);if(memberships?.length)redirect('/dashboard');const requests=await supabaseFetch(`/rest/v1/workspace_requests?user_id=eq.${user.id}&status=in.(pending_payment,pending_review)&select=id&order=created_at.desc&limit=1`);if(requests?.[0])redirect(`/workspace-payment/${requests[0].id}`);return <main className="mx-auto max-w-xl p-6"><h1 className="text-3xl font-bold">أنشئ مساحتك في مَدار</h1><p className="mt-3 text-slate-300">مساحة الطالب مجانية، بينما تُفعّل مساحات الأعمال بعد الدفع وموافقة الإدارة.</p><OnboardingForm/></main>}
