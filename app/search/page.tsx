import PageShell from '@/components/ui/PageShell';import ProductCard from '@/components/product/ProductCard';import { PageHero, Section, EmptyState } from '@/components/ui/Section';import { publishedProducts } from '@/src/data/products';export const metadata={title:'البحث'};export default async function Page({searchParams}:{searchParams:Promise<{q?:string}>}){const q=(await searchParams).q?.trim()||'';const results=q?publishedProducts.filter(p=>(p.title+p.description+p.category).includes(q)):[];return <PageShell><PageHero eyebrow="Search" title="ابحث في منتجات مَدار" description="استخدم كلمات عربية واضحة مثل أتمتة أو Notion أو مبيعات."/><Section><form><input name="q" defaultValue={q} className="w-full rounded-2xl p-4 text-slate-900" placeholder="كلمة البحث"/></form><div className="mt-8">{q?(results.length?<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{results.map(p=><ProductCard key={p.slug} product={p}/>)}</div>:<EmptyState title="لا توجد نتائج" description="جرّب كلمة أخرى أو تصفح صفحة المنتجات."/>):<EmptyState title="ابدأ البحث" description="اكتب كلمة في مربع البحث لعرض النتائج."/>}</div></Section></PageShell>}
