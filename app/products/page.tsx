import type { Metadata } from 'next';import PageShell from '@/components/ui/PageShell';import ProductCard from '@/components/product/ProductCard';import { PageHero, Section } from '@/components/ui/Section';import { productCategories, publishedProducts } from '@/src/data/products';
export const metadata: Metadata={title:'المنتجات',description:'منتجات مَدار الرقمية للأتمتة والذكاء الاصطناعي وتشغيل الأعمال.'};
export default function ProductsPage(){return <PageShell><PageHero eyebrow="متجر MADAR" title="منتجات رقمية جاهزة لتشغيل أعمالك بوضوح" description="حافظة مختارة من القوالب والأنظمة والأدوات، مع تسليم رقمي منظم وبنية قابلة للدفع والتنزيل لاحقاً."/><Section><div className="mb-8 flex flex-wrap gap-3">{productCategories.map(c=><span className="rounded-full border border-white/10 px-4 py-2 text-sm text-[#70E4D4]" key={c}>{c}</span>)}</div><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{publishedProducts.map(p=><ProductCard key={p.slug} product={p}/>)}</div></Section></PageShell>}
