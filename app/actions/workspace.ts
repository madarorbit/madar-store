'use server';
import {revalidatePath}from'next/cache';import{requireAdmin,requireUser}from'@/src/lib/auth';import{supabaseFetch}from'@/src/lib/supabase/server';import{required}from'@/src/lib/validation';import{organizationTypes,workspaceSlug}from'@/src/lib/workspace';
export type WorkspaceActionState={success?:string;error?:string;destination?:string};
export async function createWorkspace(_previous:WorkspaceActionState,form:FormData):Promise<WorkspaceActionState>{try{await requireUser();const name=required(form.get('name'),'اسم المساحة');const slug=workspaceSlug(String(form.get('slug')||name));const type=String(form.get('type'));if(slug.length<3)throw new Error('استخدم رابطاً مختصراً من 3 أحرف إنجليزية أو أرقام على الأقل.');if(!organizationTypes.includes(type as never))throw new Error('نوع الحساب غير صالح.');if(type==='STUDENT'){await supabaseFetch('/rest/v1/rpc/create_organization',{method:'POST',body:JSON.stringify({workspace_name:name,workspace_slug:slug,workspace_type:type})});revalidatePath('/dashboard');return{success:'تم إنشاء مساحة الطالب المجانية.',destination:'/student'};}const rows=await supabaseFetch('/rest/v1/rpc/create_workspace_request',{method:'POST',body:JSON.stringify({workspace_name:name,workspace_slug:slug,workspace_type:type})});const id=rows?.id||rows?.[0]?.id;if(!id)throw new Error('لم يتم إنشاء طلب المساحة.');return{success:'تم إنشاء الطلب. أكمل بيانات الدفع لإرساله للمراجعة.',destination:`/workspace-payment/${id}`};}catch(error){return{error:error instanceof Error?error.message:'تعذر بدء المساحة. حاول مرة أخرى.'};}}
export async function submitWorkspacePayment(_previous:WorkspaceActionState,form:FormData):Promise<WorkspaceActionState>{try{await requireUser();const id=required(form.get('request_id'),'الطلب'),reference=required(form.get('payment_reference'),'مرجع التحويل');await supabaseFetch('/rest/v1/rpc/submit_workspace_payment',{method:'POST',body:JSON.stringify({target_request:id,reference})});revalidatePath('/dashboard');return{success:'تم إرسال إثبات التحويل للمراجعة. ستفتح المساحة تلقائياً بعد موافقة الإدارة.',destination:'/dashboard'};}catch(error){return{error:error instanceof Error?error.message:'تعذر إرسال بيانات الدفع.'};}}
export async function reviewWorkspaceRequest(_previous:WorkspaceActionState,form:FormData):Promise<WorkspaceActionState>{try{await requireAdmin();const id=required(form.get('request_id'),'الطلب'),decision=String(form.get('decision'));if(!['approve','reject'].includes(decision))throw new Error('القرار غير صالح.');await supabaseFetch('/rest/v1/rpc/review_workspace_request',{method:'POST',body:JSON.stringify({target_request:id,decision,reason:String(form.get('reason')||'').trim()||null})});revalidatePath('/admin/workspace-requests');revalidatePath('/admin');return{success:decision==='approve'?'تم قبول الطلب وفتح المساحة تلقائياً.':'تم رفض الطلب.'};}catch(error){return{error:error instanceof Error?error.message:'تعذر مراجعة الطلب.'};}}
export async function updateWorkspace(_previous:unknown,form:FormData){try{const user=await requireUser();const id=required(form.get('id'),'المساحة');const name=required(form.get('name'),'اسم المساحة');const type=String(form.get('type'));if(!organizationTypes.includes(type as never))throw new Error('نوع الحساب غير صالح.');const rows=await supabaseFetch(`/rest/v1/organization_members?organization_id=eq.${encodeURIComponent(id)}&user_id=eq.${encodeURIComponent(user.id)}&select=role`);if(rows?.[0]?.role!=='OWNER')throw new Error('مالك المساحة فقط يستطيع تعديل إعداداتها.');await supabaseFetch(`/rest/v1/organizations?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',body:JSON.stringify({name,type,description:String(form.get('description')||'').trim()||null,whatsapp:String(form.get('whatsapp')||'').trim()||null,website:String(form.get('website')||'').trim()||null,country:String(form.get('country')||'').trim()||null,city:String(form.get('city')||'').trim()||null})});revalidatePath('/dashboard');revalidatePath('/account/business');return{success:'تم حفظ إعدادات المساحة.'};}catch(error){return{error:error instanceof Error?error.message:'تعذر حفظ الإعدادات.'};}}
export async function manageWorkspaceMember(_previous:unknown,form:FormData){try{await requireUser();const organizationId=required(form.get('organization_id'),'المساحة');const memberEmail=required(form.get('email'),'البريد الإلكتروني');const role=String(form.get('role')||'MEMBER');const operation=String(form.get('operation')||'add');if(!['MEMBER','ADMIN'].includes(role)||!['add','remove'].includes(operation))throw new Error('طلب إدارة العضوية غير صالح.');await supabaseFetch('/rest/v1/rpc/manage_organization_member',{method:'POST',body:JSON.stringify({target_organization:organizationId,target_email:memberEmail,requested_role:role,operation})});revalidatePath('/account/business/members');return{success:operation==='add'?'تمت إضافة العضو.':'تمت إزالة العضو.'};}catch(error){return{error:error instanceof Error?error.message:'تعذر تحديث العضوية.'};}}
