import { requireAdmin } from '@/src/lib/auth';import { supabaseFetch } from '@/src/lib/supabase/server';import { EntityForm } from '@/components/admin/EntityForm';export default async function Page({params}:{params:Promise<{id:string}>}){await requireAdmin();let p;try{p=(await supabaseFetch(`/rest/v1/products?id=eq.${(await params).id}&select=*`))[0];}catch{}return <main className="mx-auto max-w-3xl p-6"><h1>تعديل منتج</h1>{p?<EntityForm kind="product" initial={p}/>:<p>غير موجود</p>}</main>}
