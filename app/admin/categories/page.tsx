/* eslint-disable @typescript-eslint/no-explicit-any */
import { requireAdmin } from '@/src/lib/auth';import { supabaseFetch } from '@/src/lib/supabase/server';import { EntityForm } from '@/components/admin/EntityForm';export const dynamic='force-dynamic';export default async function Page(){await requireAdmin();let rows:any[]=[];try{rows=await supabaseFetch('/rest/v1/categories?select=*&order=sort_order');}catch{}return <main className="mx-auto max-w-4xl p-6"><h1>التصنيفات</h1><EntityForm kind="category"/>{rows.map(x=><p key={x.id} className="mt-3">{x.name} — {x.is_active?'نشط':'معطل'}</p>)}</main>}
