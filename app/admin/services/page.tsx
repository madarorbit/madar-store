/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from 'next/link';import { requireAdmin } from '@/src/lib/auth';import { supabaseFetch } from '@/src/lib/supabase/server';export const dynamic='force-dynamic';export default async function Page(){await requireAdmin();let rows:any[]=[];try{rows=await supabaseFetch('/rest/v1/services?select=id,name,slug,price_from,status&order=updated_at.desc');}catch{}return <main className="mx-auto max-w-5xl p-6"><h1 className="text-3xl">الخدمات</h1><Link href="/admin/services/new">إضافة خدمة</Link>{rows.map(x=><p className="mt-3 rounded border p-3" key={x.id}>{x.name} — {x.price_from} — {x.status}</p>)}</main>}
