/* eslint-disable @typescript-eslint/no-explicit-any */
import { requireSuperAdmin } from '@/src/lib/auth';import { supabaseFetch } from '@/src/lib/supabase/server';import { EntityForm } from '@/components/admin/EntityForm';export const dynamic='force-dynamic';export default async function Page(){await requireSuperAdmin();let rows:any[]=[];try{rows=await supabaseFetch('/rest/v1/profiles?select=id,email,full_name,role,status&order=created_at.desc');}catch{}return <main className="mx-auto max-w-4xl p-6"><h1>المستخدمون</h1>{rows.map(x=><article className="my-4 rounded border p-3" key={x.id}><p>{x.full_name} — {x.email}</p><EntityForm kind="user" initial={x}/></article>)}</main>}
