import PageShell from'@/components/ui/PageShell';import{PageHero,Section}from'@/components/ui/Section';import{supabaseFetch}from'@/src/lib/supabase/server';import{money}from'@/src/lib/order-status';export const dynamic='force-dynamic';export const metadata={title:'تقارير التجارة'};
export default async function Page(){const orders=await supabaseFetch('/rest/v1/orders?select=status,payment_status,total,discount_total,created_at');const paid=orders?.filter((x:any)=>x.payment_status==='approved')||[],revenue=paid.reduce((n:number,x:any)=>n+Number(x.total),0),discounts=(orders||[]).reduce((n:number,x:any)=>n+Number(x.discount_total),0),review=(orders||[]).filter((x:any)=>x.payment_status==='under_review').length;return <PageShell><PageHero eyebrow="القياس التشغيلي" title="تقارير التجارة" description="صورة مباشرة عن الطلبات والتحصيل دون تعقيد محاسبي مبكر."/><Section><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['إجمالي الطلبات',String(orders?.length||0)],['طلبات مدفوعة',String(paid.length)],['إيراد معتمد',money(revenue)],['بانتظار المراجعة',String(review)]].map(([l,v])=><div key={l} className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><p className="text-sm text-slate-400">{l}</p><p className="mt-2 text-3xl font-black">{v}</p></div>)}</div><p className="mt-6 rounded-2xl border border-white/10 p-5">إجمالي الخصومات الممنوحة: <strong className="text-[#70E4D4]">{money(discounts)}</strong></p></Section></PageShell>}
