/* eslint-disable @typescript-eslint/no-explicit-any */
import{requireAdmin}from'@/src/lib/auth';import{supabaseFetch}from'@/src/lib/supabase/server';import ReviewForm from'./review-form';
export const dynamic='force-dynamic';export default async function Page(){await requireAdmin();const requests=await supabaseFetch('/rest/v1/workspace_requests?status=eq.pending_review&select=id,name,slug,type,payment_reference,payment_submitted_at,user_id,profiles!workspace_requests_user_id_fkey(email,full_name)&order=payment_submitted_at.asc');return <main className="mx-auto max-w-5xl p-6"><h1 className="text-4xl font-black">طلبات فتح المساحات</h1><p className="mt-2 text-slate-400">راجع التحويل قبل القبول؛ القبول ينشئ المساحة ويمنح العميل صلاحية المالك تلقائيًا.</p><div className="mt-8 grid gap-5">{requests?.length?requests.map((request:any)=><article key={request.id} className="rounded-3xl border border-white/10 bg-white/[.04] p-6"><div className="grid gap-3 sm:grid-cols-2"><div><h2 className="text-xl font-bold">{request.name}</h2><p className="text-slate-400">{request.type} · {request.slug}</p></div><div><p>{request.profiles?.full_name||'عميل'}</p><p className="text-slate-400">{request.profiles?.email}</p><p className="mt-2 font-bold text-[#70E4D4]">مرجع: {request.payment_reference}</p></div></div><ReviewForm id={request.id}/></article>):<div className="rounded-3xl border border-dashed border-white/15 p-10 text-center text-slate-400">لا توجد طلبات دفع بانتظار المراجعة.</div>}</div></main>}
