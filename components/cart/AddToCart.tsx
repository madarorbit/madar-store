'use client';import{useState}from'react';import{useCart,type CartItem}from'./CartProvider';
export default function AddToCart({item,label='أضف إلى السلة'}:{item:Omit<CartItem,'quantity'>;label?:string}){const{add}=useCart(),[added,setAdded]=useState(false);return <button type="button" onClick={()=>{add({...item,quantity:1});setAdded(true);setTimeout(()=>setAdded(false),1200)}} className="w-full rounded-2xl bg-gradient-to-l from-[#6C3BFF] to-[#00A98F] px-6 py-4 text-center font-black text-white transition hover:-translate-y-0.5 active:scale-[.98]">{added?'تمت الإضافة ✓':label}</button>}
