'use client'; import Link from 'next/link'; import { login,register,forgotPassword } from '@/app/actions/auth';
type Kind='login'|'register'|'forgot'; const actionByKind={login,register,forgot:forgotPassword};
export function AuthForm({kind,next}:{kind:Kind;next?:string}){const action=actionByKind[kind] as unknown as (formData: FormData) => void; const pending=false;return <form action={action} className="mx-auto max-w-md space-y-4 rounded-3xl border border-white/10 bg-white/[.04] p-6" noValidate>{kind==='register'&&<Field name="full_name" label="الاسم الكامل"/>}<Field name="email" label="البريد الإلكتروني" type="email"/>{kind!=='forgot'&&<><Field name="password" label="كلمة المرور" type="password"/>{kind==='register'&&<><Field name="confirm" label="تأكيد كلمة المرور" type="password"/><label className="flex gap-2 text-sm"><input name="terms" type="checkbox"/> أوافق على الشروط والأحكام.</label></>}</>}<input type="hidden" name="next" value={next||''}/><button disabled={pending} className="w-full rounded-xl bg-white px-5 py-3 font-bold text-[#111827] disabled:opacity-60">{pending?'جارٍ الإرسال…':kind==='login'?'تسجيل الدخول':kind==='register'?'إنشاء الحساب':'إرسال رابط الاستعادة'}</button>{kind==='login'&&<p className="text-center text-sm"><Link href="/forgot-password">نسيت كلمة المرور؟</Link> · <Link href="/register">حساب جديد</Link></p>}</form>}
function Field({name,label,type='text'}:{name:string;label:string;type?:string}){return <label className="block text-sm">{label}<input required name={name} type={type} className="mt-1 w-full rounded-xl p-3 text-slate-900" /></label>}
