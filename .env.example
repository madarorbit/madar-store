# Public base URL, no trailing slash (used for secure auth redirect URLs)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Supabase project URL and browser-safe anonymous/publishable key
NEXT_PUBLIC_SUPABASE_URL=https://rybzdpduwgnsjofolini.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only key: required only for seed and owner-promotion scripts. Never expose it to the browser.
SUPABASE_SERVICE_ROLE_KEY=
# The only address eligible for the one-time SUPER_ADMIN promotion command.
SUPER_ADMIN_EMAIL=orbit.ops.digital@gmail.com
