# التسليم الرقمي

يحفظ الأصل في bucket خاص. يطلب العميل رابطاً من الخادم؛ يتحقق الخادم من user/order/payment ثم ينشئ token عشوائياً مخزناً كبصمة، محدود الوقت والمرات. تسجل كل عملية في `downloads` و`download_events`. لا تستخدم URL عام أو إخفاء زر كضابط وصول.
